import React, {memo} from 'react';
import {View, Text} from 'react-native';

const SupportScreen = memo((): JSX.Element => {
  return (
    <View>
      <Text>Support screen</Text>
    </View>
  );
});

export default SupportScreen;
